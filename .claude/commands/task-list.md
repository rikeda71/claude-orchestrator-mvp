---
description: Display all tasks or filter by status
---

Show the task list. You can optionally filter by status (pending, in-progress, review, completed).

Run: `./scripts/core/task-manager.sh list`

After running the command, provide a brief summary of the tasks to the user.
